<html lang='en'>
    <head>
        <meta charset='UTF-8' />
        <meta name='viewport' content='width=device-width, initial-scale=1.0' />
        <meta http-equiv='X-UA-Compatible' content='ie=edge' />
        <title>Document</title>
        <link rel='stylesheet' href='/stylesheets/bootstrap.css' />
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
        <!-- navigation -->
        <nav class='navbar'>
            <div class='container'>
                <div class='navbar__container--home'>
                    <a href='/' class='btn btn-secondary navbar__container--link'>回到首頁</a>
                </div>
                <div class='navbar__container--right'>
                    <div class='btn btn-secondary'>
                        <a href='/about' class='navbar__container--link'>About</a>
                    </div>
                    <div class='btn btn-secondary'>
                        <a href='/portfolio' class='navbar__container--link'>Portfolio</a>
                    </div>
                    <div class='btn btn-secondary'>
                        <a href='/contact' class='navbar__container--link'>Contact</a>
                    </div>
                </div>
            </div>
        </nav>

        <!-- partial templates will replace the part of "body" here -->
        {{{body}}}
        <script src='/javascripts/popper.js'></script>
        <script src='/javascripts/bootstrap.js'></script>

    </body>
</html>